<%= form_with model: @user do |f| %>

  <div class="field">
    <%= f.label :bio %><br />
    <%= f.text_area :bio, autofocus: true %>
  </div>

  <div class="actions">
    <%= f.submit "Update" %>
  </div>

<% end %>